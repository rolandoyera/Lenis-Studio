---
title: Tests
order: 9
---

# Tests

This page documents the current testing setup, what has already been covered,
what still needs coverage, and how we should decide what to test next. Treat it
as a living plan: when behavior, rules, lifecycle steps, or tooling changes,
update this page in the same change.

The testing goal is not broad visual coverage. The priority is to catch real
breakage in business behavior, data safety, organization isolation, lifecycle
rules, contract logic, and reference-code generation.

## Current Stack

The dashboard uses a layered testing setup:

- **Vitest** for fast unit and integration tests.
- **React Testing Library** only where component behavior matters.
- **Firebase Emulator + `@firebase/rules-unit-testing`** for Firestore security
  rules.
- **Playwright** for a small smoke suite around critical flows.

Available scripts:

```json
{
  "test": "vitest",
  "test:run": "vitest run",
  "test:rules": "vitest run --config vitest.rules.config.ts",
  "test:rules:emulator": "firebase emulators:exec --project sdg-rules-test --only firestore,storage \"npm run test:rules\"",
  "test:e2e": "playwright test"
}
```

Run commands from `apps/dashboard`:

```powershell
npm run test:run
npm run test:rules:emulator
npm run test:e2e
npm run build
```

## What Is Covered

### Pure Helpers

Coverage exists for shared helpers that are easy to break and cheap to test:

- `src/lib/utils.test.ts`
  - currency display and editable currency input formatting
  - US phone normalization, formatting, and validation
  - vendor phone formatting for US/CA and international-style input
  - ZIP formatting and validation
  - tax ID normalization and formatting
- `src/server/reference-codes.test.ts`
  - reference-code formatting
  - sequence allocation
  - missing-counter fallback behavior

### Form Schemas And Transforms

Coverage exists for schema validation and data-shaping helpers:

- `lead-constants.test.ts`
- `library-constants.test.ts`
- `project-constants.test.ts`
- `tenant-schema.test.ts`
- `user-schema.test.ts`
- `vendor-constants.test.ts`
- `profile-payloads.test.ts`

These tests focus on required fields, sentinel cleanup, normalized persistence
payloads, form defaults, and legacy data mapping.

### Contract Helpers

Contract helper tests cover the fixed code-based template and payload builders:

- `contract-template.test.ts`
  - bold/token parsing
  - token extraction order
  - date formatting
  - client and company address formatting
- `build-contract-payload.test.ts`
  - lightweight draft payloads
  - frozen `lockedSnapshot` payload shape
  - resolved parties and project snapshot data
- `contract-resend.test.ts`
  - `contractResendEligibility` / `canResendContract` — the **single source of truth** for "can this
    contract's signing link be resent?" shared by the server guard and every UI affordance (builder,
    contracts list, project Files tab). Covers the eligible states (sent/viewed/expired + locked) and
    every rejection branch (never-sent/no-lock, executed by timestamp or status, voided). Locking this
    matters because the rule gates a security-sensitive action in four places and must not drift.
- `brevo.test.ts`
  - `normalizeBrevoWebhookEvent` — the raw-event → audit-type mapping that produces the delivery
    signal. Covers bounce → `email_bounced` and blocked/invalid/spam/error → `email_blocked` (the two
    that the webhook route turns into the contract's **Delivery Failed** status), sent/delivered, the
    null cases (opens/clicks/deferred/unsubscribed and missing event), and metadata/recipient/id
    extraction from the `X-Mailin-custom` header (incl. the `tags` fallback and bad-JSON handling).

### Server Actions With Mocked Firebase Admin

Server-action behavior is covered with mocked Firebase Admin dependencies:

- `create-actions.test.ts`
  - client creation with active-org ownership and server reference codes
  - project creation with active-org ownership and copied client code
  - draft contract creation with server lifecycle fields
  - lead conversion
  - missing active-org rejection branches
- `contract-signing.test.ts`
  - send-for-signature
  - server-derived signer and recipient
  - portal access creation
  - audit and email calls
  - sign validation branches
  - final artifact pipeline writes
  - failure branches that must not produce side effects
- `portal.test.ts`
  - hashed token storage
  - salted phone-last-4 challenge data
  - TTL calculation
  - identity mismatch hiding
  - expired-token org-name messaging
- `invite-actions.test.ts`
  - admin invite flow
  - server-built mail enqueueing
  - contributor denial
  - cross-org resend denial
  - tenant creation with pending admin invite

### Storage Upload Helpers

Coverage exists for the client-side upload helpers in `src/lib/db.ts`:

- `db.image-cache.test.ts`
  - every upload helper (`uploadLibraryImage`, `uploadLibraryImageBlob`,
    `uploadVendorImage`, `uploadVendorImageBlob`, `uploadOrgBrandingImage`) sets
    the immutable `Cache-Control` header (`public, max-age=31536000, immutable`)
    on `uploadBytes`
  - library blob uploads use the unified `images/{id}` path with no separate
    `cover.{ext}` file, and honor an explicit image id/extension

This guards image freshness: because a changed image always gets a new download
URL, the bytes are safe to cache immutably and never go stale. It tests the
header/path contract with a mocked `firebase/storage`, not access control —
access control is the Storage rules suite's job (see below).

### Firestore Query Shape (N+1 Guardrail)

Coverage exists for the read-efficiency contract of the list getters in
`src/lib/db.ts`:

- `db.query-shape.test.ts`
  - every list getter (`getClients`, `getLeads`, `getVendors`, `getProjects`,
    `getLibraryItems`, `getVendorLibraryItems`, `getProposals`, `getContracts`,
    `getTrades`, `getOrganizationUsers`, `getProjectRooms`,
    `getProjectRoomItems`, `getClientNotes`, `getProjectNotes`) issues exactly
    one `getDocs` query, zero per-document `getDoc` calls, and queries exactly
    one collection (with the expected path, including notes subcollections)

Two deliberate design choices make this a real guardrail:

- It asserts **calls issued, not documents read**. Document counts scale with
  data, so they are not a stable contract; the number of Firestore calls scales
  with code, and that is what an N+1 regression changes.
- The mocked query result is **non-empty** (three docs). A regression that
  fetches a document per result row is invisible against an empty result set.

When adding a new list getter to `db.ts`, add it to the test's table. If a
getter legitimately needs a second query, change its expectations deliberately —
the point is that query count is a reviewed contract, not an accident.

Page-level read volume (many getters composed on one page) is intentionally not
tested here; that remains the job of the dev-only `window.__dbStats()` tracing
from `src/lib/db-trace.ts`.

### Component Effect Behavior

The first React Testing Library component test guards the effect-dependency
rule that lint cannot: data effects must key on stable primitives from
`useAuth` (`organizationId`, `authLoading`), never the `profile` object, whose
identity churns on every profile `onSnapshot` heartbeat. Biome's
`useExhaustiveDependencies` passes profile-keyed deps (they are "exhaustive",
just unstable), so this class of bug needs a behavioral test.

- `clients/page.test.tsx` (next to the clients directory page; the template for
  future component tests)
  - fetches once per `organizationId`, and a rerender with a new-but-equal
    `profile` object (the simulated heartbeat) must **not** refetch
  - a genuine `organizationId` change **must** refetch
  - no fetch while auth is still loading

The suite was mutation-verified: rekeying the page effect on `profile` fails
both directions (spurious refetch on heartbeat, missed refetch on org change).

Template notes for covering another page:

- `useAuth` is mocked via a mutable module-level `authState` so rerenders can
  simulate context churn.
- The feature's `src/server/*-actions.ts` module must be mocked — server
  actions import Firebase Admin, which cannot load in jsdom.
- Wrap the page in the real `PageTitleProvider` (with `next/navigation`'s
  `usePathname` mocked); `<PageTitle>` throws without it.

### Firestore Rules

Firestore security rule coverage lives in `firestore/rules.test.ts` and runs
against the emulator. Current coverage includes:

- org isolation
- contract draft-only client update/delete rules
- denied client-side contract creation
- locked/sent contract mutation denial
- server-only contract audit access
- server-only project document writes
- portal access client-write denial
- unauthenticated portal client-SDK denial
- invite and self-profile rules
- role and organization escalation denial
- organization secrets denial
- reference counter denial
- mail outbox denial

### Storage Rules

Storage security rule coverage lives in `storage/rules.test.ts` and runs
against the emulators via `npm run test:rules:emulator`. The suite needs
**both** the Storage and Firestore emulators because `storage.rules` resolves
the uploader's org through a cross-service `firestore.get` of
`users/{uid}.organizationId` — each test seeds the user profile into the
Firestore emulator first. Current coverage includes:

- org-scoped branding, library, and vendor image uploads succeed for the
  uploader's own org
- cross-org write denial on all three roots
- unauthenticated write denial
- non-image upload denial (contentType must match `image/*`)
- size cap: exactly 15 MiB denied, just under allowed
- signed-in read behavior (reads only require sign-in; org isolation is
  enforced on writes) and unauthenticated read denial
- deletes allowed in the uploader's own org, denied cross-org
- default-deny for paths outside the three known roots

Both rules suites share the emulator project and clear it between tests, so
`vitest.rules.config.ts` sets `fileParallelism: false` to run the files
sequentially.

### Documentation

Feature notes were updated where tests are most relevant:

- Contracts notes document contract test coverage and rules coverage.
- Profile notes document invite/mail security invariants.
- Instagram notes document server-render failure behavior.
- Clients notes document the effect-churn component test and mark the
  `organizationId`-dependency convention as test-enforced.
- The root `AGENTS.md` verification rules reference the query-shape and
  effect-dependency guardrails.

## Important Lessons From Production

The Firebase Admin SDK 14 incident showed a test gap: unit tests and local
builds did not catch a production server bundle/runtime failure.

The failure was not caused by tests. It came from the runtime dependency chain:

```text
firebase-admin@14 -> jwks-rsa@4 -> jose@6
```

In Vercel production, `firebase-admin/auth` loaded through a CommonJS `require`
path and failed on ESM-only `jose`. The fix was to roll `firebase-admin` back to
`13.10.0` while leaving the client Firebase SDK and test tooling upgrades in
place.

Testing implication: dependency upgrades that affect server runtime packages
need preview-deployment smoke checks, not only local Vitest and `next build`.

## Strategy

Use the smallest test that protects the behavior at risk:

- **Pure helper or schema behavior:** Vitest unit tests.
- **Transform or payload logic:** Vitest integration-style tests around the
  helper boundary.
- **Server action behavior:** Vitest with mocked Firebase Admin, mocked
  third-party providers, and assertions against side effects.
- **Firestore call shape (N+1 risk):** Vitest with mocked `firebase/firestore`,
  asserting the number of calls issued — never the number of documents read.
- **Effect dependency churn:** React Testing Library component test with a
  mocked `useAuth`, rerendering with new-but-equal objects and asserting fetch
  counts.
- **Firestore access control:** Firebase Emulator rules tests.
- **Storage access control:** Firebase Storage Emulator rules tests.
- **Critical production route health:** Playwright smoke tests or preview
  deployment checks.

Prefer behavior over implementation details. A good test should explain a
business or security rule in plain language and fail when that rule is broken.

## What Remains

### 1. Project Room Firestore Rules

The Firestore rules for project rooms and project room items inherit org access
from the parent project instead of relying only on direct `organizationId`
checks. That makes them worth focused emulator coverage.

Add tests for:

- org member can read/update/delete a room for an own-org project
- cross-org room access is denied
- room creation requires an own-org parent project
- item creation requires an own-org parent project
- item creation requires matching `organizationId`
- default-deny for orphaned or mismatched project ids

### 2. Production Route Smoke Coverage

Add a small smoke suite that catches server-rendered route crashes. Keep it
small and behavior-focused, not visual.

Start with:

- auth gate or dashboard redirect behavior
- `/dashboard/instagram`
- `/dashboard/contracts`
- one contract detail route when fixtures exist
- one portal route when fixtures exist

For dependency updates, prefer running smoke checks against a Vercel preview
deployment because local builds can miss production bundling issues.

### 3. Firebase Admin Runtime Smoke

Add a small server-side smoke check for `firebase-admin.ts` import/accessor
behavior. This will not fully reproduce Vercel's bundle/runtime, but it can
catch obvious import regressions before deployment.

The stronger protection is still preview-deployment smoke coverage for routes
that import server modules using Firebase Admin.

### 4. Contract Post-Send Lifecycle

Send/sign coverage is strong. The next contract lifecycle risks are
administrative post-send actions.

Add tests when those flows exist or change:

- void sent contract
- revoke portal access
- ensure void/revoke are server-only
- ensure audit entries are append-only
- ensure completed portal links remain viewable where intended

## Future Testing Work

Future work should stay layered and risk-driven:

- Add project-room rule coverage before adding more schema tests. (Storage
  Emulator tests are done — see "Storage Rules" under What Is Covered.)
- Add a tiny Playwright smoke suite before broad component coverage.
- Add server-action tests whenever a new server action writes multiple
  collections or sends external effects.
- Add rules tests whenever a rule changes or a new client-written collection is
  introduced.
- Add regression tests for any production incident after the root cause is
  understood.

Avoid:

- broad visual snapshot suites
- tests that assert internal React state instead of user-visible behavior
- duplicating Firestore rule logic in unit tests instead of using the emulator
- live third-party API calls in unit tests
- testing every component just because it renders

## SDK Upgrade Policy

Runtime SDK upgrades need their own checklist because local tests may pass while
production server bundling fails.

For Firebase SDK upgrades:

1. Upgrade one runtime package at a time when possible.
2. Keep `firebase`, `firebase-admin`, and test/emulator packages conceptually
   separate in the change description.
3. Run unit tests.
4. Run Firestore/Storage emulator tests when rules or SDK access patterns
   change.
5. Run `npm run build`.
6. Deploy to a Vercel preview.
7. Smoke test server-rendered routes that import Firebase Admin.
8. Check Vercel runtime logs before merging to `main`.

For `firebase-admin@14`, do not reintroduce it directly on `main`. Test it in a
branch or preview deployment first, specifically checking routes and actions
that import `firebase-admin/auth`.
